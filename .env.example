NEXT_PUBLIC_SITE_URL=https://bexhearts.com
NEXT_PUBLIC_SUPABASE_URL=
# Required for the waitlist API route (inserts bypass RLS via service role;
# the table deliberately has no anon policies — see supabase/waitlist_rls_fix.sql).
SUPABASE_SERVICE_ROLE_KEY=
# Not used by the waitlist; safe to leave empty.
NEXT_PUBLIC_SUPABASE_ANON_KEY=
